# Environment variables — copy to .env and fill in
# Never commit .env to git

# API settings
PORT=8000
ENVIRONMENT=development   # development | production

# Stripe (for billing — add later)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Max file size in MB
MAX_UPLOAD_MB=50
